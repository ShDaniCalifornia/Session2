<Window x:Class="Session2.View.Windows.AuthorizationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Session2.View.Windows"
        mc:Ignorable="d"
        Title="Окно авторизации" 
        SizeToContent="WidthAndHeight"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="50"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="50"/>

        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="10"/>
            <ColumnDefinition Width="300"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Логин" VerticalAlignment="Center"/>

        <TextBlock Grid.Row="2" Text="Пароль" VerticalAlignment="Center"/>

        <Button Content="Войти" Name="LoginBtn" Grid.Column="2" Grid.Row="4" Click="LoginBtn_Click"/>

        <TextBox Grid.Column="2" Name="LoginBl" />

        <PasswordBox Grid.Column="2" Grid.Row="2" Name="PasswordPb"/>

    </Grid>
</Window>
